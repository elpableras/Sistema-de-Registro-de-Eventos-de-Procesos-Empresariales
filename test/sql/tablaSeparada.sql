CREATE TABLE Registro (id int(6) NOT NULL AUTO_INCREMENT PRIMARY KEY, codigo varchar(30) NOT NULL, nombre varchar(40) NOT NULL, email varchar(20), edad int(2), url varchar(100), mensj_cifrado blob);
