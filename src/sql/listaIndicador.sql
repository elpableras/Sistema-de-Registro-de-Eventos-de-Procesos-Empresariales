CREATE TABLE lIndicador (codRegistro VARCHAR(12) NOT NULL PRIMARY KEY, indicador TEXT NOT NULL, borrado VARCHAR(1) NOT NULL,
FOREIGN KEY (codRegistro) REFERENCES general(codGeneral) ON DELETE CASCADE ON UPDATE CASCADE) ENGINE=InnoDB;