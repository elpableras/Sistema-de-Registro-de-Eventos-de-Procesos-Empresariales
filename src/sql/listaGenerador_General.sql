CREATE TABLE lGenerador_Gene (codGeneral VARCHAR(12) NOT NULL PRIMARY KEY, generador TEXT NOT NULL, borrado VARCHAR(1) NOT NULL,
FOREIGN KEY (codGeneral) REFERENCES general(codGeneral) ON DELETE CASCADE ON UPDATE CASCADE) ENGINE=InnoDB;