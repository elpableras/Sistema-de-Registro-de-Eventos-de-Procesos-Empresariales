CREATE TABLE lAutorizado_Gene (codGeneral VARCHAR(12) NOT NULL PRIMARY KEY, autorizado TEXT NOT NULL, borrado VARCHAR(1) NOT NULL,
FOREIGN KEY (codGeneral) REFERENCES general(codGeneral) ON DELETE CASCADE ON UPDATE CASCADE) ENGINE=InnoDB;