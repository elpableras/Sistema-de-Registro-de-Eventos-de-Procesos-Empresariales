CREATE TABLE lDocumento_Gene (codGeneral VARCHAR(25) NOT NULL PRIMARY KEY, enlace TEXT NOT NULL, tipoDescripcion TEXT NOT NULL, borrado VARCHAR(1) NOT NULL,
FOREIGN KEY (codGeneral) REFERENCES general(codGeneral) ON DELETE CASCADE ON UPDATE CASCADE) ENGINE=InnoDB;